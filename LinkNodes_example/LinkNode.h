#ifndef _listnode_h
#define _listnode_h

//this will be the  structure where we will build upon
struct Listnode {
	int data;	//this will store the data which we will provide
	Listnode* next; //this will point to next node
};

#endif
